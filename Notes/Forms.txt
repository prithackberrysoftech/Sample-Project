

1️ What is a Django Form (in simple words)

	A Django Form is used to:

	✔ take user input
	✔ validate data
	✔ clean data
	✔ show errors nicely
	✔ save data (optionally)
	
	
2️ Types of Forms in Django

	There are 2 main types you must know:

		1. forms.Form

		👉 Use when:

		No database model
		Login form
		Contact form
		Search form
		
		~~~~~~~~~~~
		| Method              | Use               |
		| ------------------- | ----------------- |
		| `request.POST`      | submitted data    |
		| `form.is_valid()`   | runs validation   |
		| `form.cleaned_data` | safe cleaned data |
		| `form.errors`       | error messages    |


		~~~~~~~~~
		form.is_valid()


		2. forms.ModelForm

		👉 Use when:

		Data is linked to a model

		CRUD forms (Create / Update)
		

3. Field-Level Validation (clean_fieldname)

	Example: Validate Name
	
		class ContactForm(forms.Form):
			name = forms.CharField(max_length=50)

			def clean_name(self):
				name = self.cleaned_data.get('name')

				if len(name) < 3:
					raise forms.ValidationError("Name must be at least 3 characters")

				return name


	✔ This runs only for name field


4.Form-Level Validation (clean())

	Used when: Validation depends on multiple fields

	Example: Email + Name check
	def clean(self):
		cleaned_data = super().clean()
		name = cleaned_data.get('name')
		email = cleaned_data.get('email')

		if name and email and name.lower() in email.lower():
			raise forms.ValidationError("Email should not contain your name")

		return cleaned_data
		
		
5. Showing Errors in Template


{% if form.errors %}
    <ul>
        {% for field in form %}
            {% for error in field.errors %}
                <li>{{ error }}</li>
            {% endfor %}
        {% endfor %}
    </ul>
{% endif %}


6. Common Form Fields You MUST Know
	
	| Field          | Purpose     |
	| -------------- | ----------- |
	| `CharField`    | text        |
	| `EmailField`   | email       |
	| `IntegerField` | number      |
	| `DecimalField` | price       |
	| `ChoiceField`  | dropdown    |
	| `BooleanField` | checkbox    |
	| `DateField`    | date        |
	| `FileField`    | file upload |



====================
Note : forms.ModelForm in functionbased app in views_django project 
====================
